services:
  - type: web
    name: margn-backend
    env: docker
    dockerfilePath: ./apps/backend/Dockerfile
    dockerContext: .
    envVars:
      - key: NODE_ENV
        value: production
    healthCheckPath: /api/health
    plan: free
