{
  "name": "margn-scripts",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "prisma:generate": "prisma generate --schema=../../apps/backend/prisma/schema.prisma",
    "seed:demo": "NODE_PATH=../../node_modules npx ts-node seed-demo-portfolio.ts",
    "prices:ingest": "NODE_PATH=../../node_modules npx ts-node price-ingestion.ts",
    "metrics:refresh": "NODE_PATH=../../node_modules npx ts-node metrics-refresh.ts",
    "daily:check": "NODE_PATH=../../node_modules npx ts-node daily-check.ts",
    "daily:all": "NODE_PATH=../../node_modules npx ts-node run-daily-jobs.ts",
    "reset:database": "NODE_PATH=../../node_modules npx ts-node reset-database.ts",
    "explore:portfolios": "NODE_PATH=../../node_modules npx ts-node explore-portfolios.ts"
  },
  "dependencies": {
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "prisma": "^5.22.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.2"
  }
}
