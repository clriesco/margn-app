{
  "installCommand": "cd ../.. && npm ci",
  "buildCommand": "ROOT=$(cd ../.. && pwd) && cd $ROOT && npm --workspace apps/backend run prisma:generate && npm --workspace packages/shared run build && npm --workspace apps/frontend run build",
  "outputDirectory": ".next",
  "framework": "nextjs"
}
